using System;

namespace AtividadesProgramacao
{
    class Exercicio02
    {
        /*        
                  static void Main(string[] args)
                {
                    double valorHoraAula;
                    int numeroAulasDadas;
                    double porcentagemDescontoINSS;
                    double salarioLiquido;
                    double desconto;

                    Console.WriteLine("Digite o valor da hora aula: ");
                    double.TryParse(Console.ReadLine(), out valorHoraAula);

                    Console.WriteLine("Digite o quantidade de aulas dadas: ");
                    int.TryParse(Console.ReadLine(), out numeroAulasDadas);

                    Console.WriteLine("Digite a porcentagem de desconto do INSS: ");
                    double.TryParse(Console.ReadLine(), out porcentagemDescontoINSS);

                    desconto = (valorHoraAula * numeroAulasDadas) * (porcentagemDescontoINSS / 100);

                    salarioLiquido = (valorHoraAula * numeroAulasDadas) - desconto;

                    Console.WriteLine("O Salario liquido do professor é: " + salarioLiquido);
                    Console.WriteLine("O Valor do desconto do INSS é: " + desconto);
                }
        */
    }
}
