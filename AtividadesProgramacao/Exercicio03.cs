using System;

namespace AtividadesProgramacao
{
    class Exercicio03
    {
        /*
               static void Main(string[] args)
               {
                   int numero;
                   double resultado;

                   Console.WriteLine("Digite um numero: ");
                   int.TryParse(Console.ReadLine(), out numero);

                   if (numero > 0)
                   {
                       resultado = Math.Sqrt(numero);
                       Console.WriteLine("O numero digitado é positivo e a raiz quadrada é: " + resultado);
                   }
                   else {
                       resultado = numero * numero;
                       Console.WriteLine("O numero digitado é negativo e o quadrado do numero é: " + resultado);
                   }
       */
    }
}
